# 当前工作

## 待办

- [x] 审视现有本地数据、侧边栏、筛选和管理弹窗结构。
- [x] 新增本地工作区存储，支持导出、导入和清理。
- [x] 增加工作区筛选逻辑，支持文件夹、标签、搜索词并集展示。
- [x] 在侧边栏增加工作区入口和管理入口。
- [x] 增加工作区管理弹窗，支持创建、编辑、删除。
- [x] 补充工作区存储和筛选测试。
- [x] 更新 README 和 `docs/plan.md`。
- [x] 使用 `nvm use v22` 运行验证。
- [x] 记录结果。

## 结果

- 已新增本地工作区存储，工作区包含名称、绑定文件夹、绑定标签和可选搜索词。
- 工作区已接入本地数据导出、导入和清理流程。
- 已新增工作区筛选逻辑：文件夹和标签取并集，工作区搜索词和用户搜索词继续叠加过滤。
- 侧边栏已新增工作区列表和“管理”入口。
- 已新增工作区管理弹窗，支持创建、编辑、删除工作区。
- 已更新 README 和 `docs/plan.md`，说明工作区是本地筛选入口，不修改浏览器书签树。
- 已补充工作区存储、导出/导入和筛选测试。
- 验证通过：`source ~/.nvm/nvm.sh && nvm use v22 && pnpm test`，14 个测试文件、103 个测试通过。
- 验证通过：`source ~/.nvm/nvm.sh && nvm use v22 && pnpm verify`。
- 验证通过：`git diff --check`。
- manifest 抽查通过：`.output/chrome-mv3/manifest.json` 的 `permissions` 只有 `bookmarks`，没有 `content_scripts`。
