# 当前工作

## 待办

- [x] 审视现有本地数据导出/导入、书签卡片操作和筛选数据流。
- [x] 新增本地标签存储，支持保存、读取、删除、导出、导入和清理。
- [x] 增加单个书签“编辑标签”入口和标签编辑弹窗。
- [x] 增加标签筛选入口，并保证搜索在标签筛选结果内生效。
- [x] 补充标签存储和标签筛选测试。
- [x] 更新 README 和 `docs/plan.md`。
- [x] 使用 `nvm use v22` 运行验证。
- [x] 记录结果。

## 结果

- 已新增本地标签存储，标签按书签 id 保存，空标签会过滤，重复标签会去重。
- 已将标签接入本地数据导出/导入和清理本地数据流程。
- 已在书签卡片菜单中增加“标签”入口，并新增标签编辑弹窗。
- 已在侧边栏增加标签筛选入口；搜索会在当前标签筛选结果内继续匹配。
- 已在书签卡片上展示已有标签。
- 已更新 README 和 `docs/plan.md`，说明标签独立于浏览器文件夹并只保存在本地。
- 验证通过：`source ~/.nvm/nvm.sh && nvm use v22 && pnpm verify`，12 个测试文件、89 个测试通过。
- 验证通过：`git diff --check`。
- manifest 抽查通过：`.output/chrome-mv3/manifest.json` 的 `permissions` 只有 `bookmarks`，没有 `content_scripts`。
